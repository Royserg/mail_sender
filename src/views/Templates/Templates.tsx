import { FC } from 'react';
// Components
import ViewContainer from 'components/ViewContainer';

const Templates: FC = () => {
  return (
    <ViewContainer heading='Email templates'>
      <p>Saved Templates</p>
      <p>Create Template</p>
    </ViewContainer>
  );
};

export default Templates;
