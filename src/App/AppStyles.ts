import { makeStyles } from '@material-ui/core';
import { sizes } from '../assets/jss/general';

const appStyles = makeStyles({
  main: {
    marginLeft: sizes.sidebarWidth,
  },
});

export default appStyles;
